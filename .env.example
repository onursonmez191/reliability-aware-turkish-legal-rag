# Optional. Only needed when using Hugging Face Inference API instead of Ollama.
# Never commit a real token in .env.
HF_API_TOKEN=

# Optional. Uncomment to use a non-default config file.
# RAG_CONFIG=configs/default.yaml
